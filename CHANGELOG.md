# 版本记录

本文件记录用户可见的重要变化。版本号是网页/PWA 发布版本，不要求对应 Git tag。

## [v26] - 2026-08-14

### 新增

- 设置中增加“完整备份与恢复”：一次导出全部对话、System、智能体状态、草稿和 IndexedDB 附件，可在另一台电脑的同一网页中追加恢复。
- 完整备份不包含 API Key、连接配置或本机登录；导入会重建对话、消息和附件 ID，同一备份重复导入不会生成副本。
- 本机 Codex 增加“同时保存到 Codex”开关。默认关闭时通过 App Server 创建 `ephemeral` 临时任务，只保留网页侧记录；主动开启后才写入 Codex 最近任务。

### 修复

- 修复窄窗口和已安装 PWA 中，搜索/多选工具条隐藏后网格顺序错位，导致输入区下方留出大片空白的问题。
- 聊天面板现在始终占满可见高度，消息区弹性伸缩，输入区固定贴住窗口底部。
- 修复 Safari 中长说明挤压自定义开关，导致“同时保存到 Codex”只剩圆形滑块、无法辨认开关状态的问题。
- Safari / Safari Web App 选择本机工具时不再反复执行注定失败的检测；页面会说明 WebKit 阻止线上 HTTPS 页面访问 `127.0.0.1`，并提供复制网址到 Edge/Chrome 普通标签页的入口。

### 改进

- System 提示词不再以常驻多行文本框显示；聊天区只保留“未设置 / 已设置”状态和编辑按钮。
- 点击“编辑”会打开独立弹窗；取消不保存草稿，保存后仅更新当前对话，手动改写时会取消已选智能体绑定。
- 窄窗口中 System 编辑弹窗使用全屏布局，并保持焦点返回和 `Esc` 取消行为。
- 本机 Codex 在临时任务字段不受旧 CLI 支持时会安全失败并提示升级，不会静默回退为可见任务。
- 明确 Safari Web App 适合稳定使用第三方中文输入法与云端 API；本机 Codex、Gemini CLI 等桥接仍需 Edge/Chrome 普通标签页，避免把浏览器传输限制误判成脚本或本地网络权限故障。
- 在公开版本仍为 v26 的候选阶段，把 PWA shell/runtime 缓存推进到 `v26-r3`，确保已安装应用能发现本次同版本内容更新。

## [v25] - 2026-08-13

### 新增

- 自定义智能体的 System 定义支持安全 Markdown 实时预览，保存与应用时仍保留 Markdown 原文。
- macOS Edge PWA 内增加第三方输入法长期迁移提示，可复制地址并按步骤创建 Safari Web App。
- 增加消息单条复制、编辑并重新发送、自动分支、重新生成、继续生成、失败重试和从此处删除。
- 增加当前对话搜索、重命名、Markdown/JSON 导出，以及消息多选复制、导出和删除。
- 增加图片、文本/代码和 PDF 附件；PDF 使用自托管 PDF.js 在浏览器本地提取文字，附件内容持久保存到 IndexedDB。
- 连接检查会发送一张极小测试图片验证当前配置的多模态能力；仅通过验证的配置显示附件按钮。

### 改进

- 本机桥接启动后由现有页面自动发现并连接，包括启动器自动选择的替代端口，不再重复弹出网页窗口。
- 只有未检测到现有页面时才唤起已安装的桌面应用；macOS 找不到已安装应用时才回退到默认浏览器。
- OpenAI Compatible、Anthropic Messages 和 Gemini 均支持正确的图片请求结构，检查器会隐藏 base64 图片数据。
- 前端和 Cloudflare Worker 统一检查完整请求不超过 20 MiB，容纳 12 MiB 附件的 base64 膨胀，并在历史附件累积过大时提前提示。
- PWA 在不改变公开版本号的情况下使用 `v25-r2` 内部缓存修订，并禁用 Service Worker 脚本的 HTTP 缓存复用，确保同版本候选包也能替换旧资源。
- 修复清空文件输入框时 Chromium 的实时 `FileList` 同步变空，导致已选择附件未进入处理流程的问题。

### 文档

- 记录 Edge 更新重启后候选窗故障会复发，明确重装 Edge PWA 只是临时恢复，Safari“添加到程序坞”才是不依赖 Edge App Shim 的长期方案。
- 将启动器回跳规则和输入法长期方案写入维护者工作约定，避免以后回退到重复开窗或无效的前端修复。

## [v24] - 2026-08-13

### 文档与收尾

- 重构 README 为产品首页，增加清晰的快速开始、能力矩阵、架构图和文档导航。
- 建立完整文档中心，拆分使用、本机 AI、故障排查、部署发布与架构说明。
- 增加安全策略、贡献指南和正式版本记录。
- 固化 GitHub、PWA、Cloudflare ZIP、版本同步与发布后检查流程。
- 将项目状态调整为稳定维护阶段；计划功能均已有明确边界或后续路线。

### 发布

- 同步网页、本机桥接、Worker、manifest 和 Service Worker 为 v24。
- 新建并验证 `AI-Shakedown-Console-cf-pages-worker-v24.zip`。

## [v23] - 2026-08-13

### 修复

- 修复 macOS/Safari 中文输入法候选词确认可能被快捷键误判的问题。
- 独立跟踪组合输入状态，并兼容 `keyCode 229`。
- 将版本号从右下角移到左上角副标题下方，避免遮挡操作。

## [v22] - 2026-08-13

### 新增

- 增加 PWA manifest、Service Worker、完整安装图标和安装入口。
- 支持独立窗口运行、离线打开应用外壳和版本更新提示。
- 固化 PWA 缓存版本与发布校验规则。

## [v21] - 2026-08-13

### 改进

- 完成专注聊天工作流：连接成功后自动收起设置与请求检查器。
- 对话超过 4 个后切换到左侧列表并持久保持。
- 将自定义对话角色并入智能体库，分为内置与自定义。
- 设置与对话复用同一个可切换左侧区域。

## [v13–v20] - 2026-08-12 至 2026-08-13

### 本机能力与交互

- 增加 Codex、Antigravity、Gemini CLI、Claude Code 和 OpenCode 本机桥接。
- 增加 macOS、Windows、Linux 自检启动器和自动系统识别。
- 增加 Node.js/Codex 内置运行时发现、旧桥接停止和端口自动避让。
- 桥接成功后转入后台，可关闭终端并从网页停止。
- 修复 Codex 沙盒值为官方支持的 `read-only`。
- 增加本地 Codex、Gemini CLI、Claude Code 和通用 JSON/JSONL 对话导入。
- 增加首次环境帮助、快捷键说明和 macOS 下载后教程。
- 增加 268 个中文内置智能体和自定义智能体编辑。

## [v9–v12] - 2026-08-11

### 核心工作台

- 完成 Cloudflare Pages Worker 同域代理和上游白名单。
- 增加配置持久化、配置库、多对话和模型列表读取。
- 增加 OpenAI Compatible、Anthropic 和 Gemini 协议适配。
- 增加流式响应、安全 Markdown、请求检查、Token 与费用统计。
- 固化自托管依赖和 Cloudflare Pages ZIP 发布方式。

## 早期版本 - 2026-01-26

- 建立可用的单页 AI API 调试界面。
- 完成基础连接、消息发送和响应展示。

[返回项目首页](./README.md) · [部署与发布](./docs/DEPLOYMENT.md)
