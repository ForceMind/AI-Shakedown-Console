# 安全策略

AI Shakedown Console 同时接触浏览器凭据、远程 API 和本机 CLI。请先理解以下边界，再决定部署位置和使用数据的敏感程度。

## 支持范围

安全修复以当前 `main` 和最新发布版为准。历史 ZIP 保留用于追溯，不承诺回补安全更新。

| 版本 | 状态 |
| --- | --- |
| v27 / `main` | 支持 |
| v26 及更早 | 仅历史存档，建议升级 |

## 凭据与浏览器存储

- API Key 和连接配置会保存在当前站点的 `localStorage`，没有额外加密。
- 附件内容会保存在当前站点的 `IndexedDB`，同样没有额外加密；对话只保存附件引用和元数据。
- 同源脚本、具有页面访问权的浏览器扩展或 XSS 都可能读取这些内容。
- 只在可信部署、个人设备和受控浏览器中保存生产密钥。
- 不要在共享电脑、公共浏览器或不受信任的二次部署中保存真实凭据。
- 使用完共享设备后，点击“清除全部本地数据”，并在服务商侧轮换可能暴露的 Key。

项目不会把 API Key 写入仓库、ZIP 或 Cloudflare 环境变量。同域代理只在请求期间把浏览器提供的认证转发给允许的上游。

## 附件与多模态边界

- “检查连接”会把项目内置的 16×16 PNG 发送给当前模型，用于验证图片输入能力；它不包含用户文件，但可能产生极小的 API 用量。
- 用户选择的图片会作为 base64 多模态内容发送；文本/代码会读取原文，PDF 只发送浏览器本地提取的文字，不发送原 PDF 二进制。
- 请求检查器隐藏图片 base64，但会显示文本附件内容；复制检查信息前仍要按业务敏感度审查。
- 不支持或未验证多模态的配置不会显示附件按钮。本机 CLI 桥接也不会接收网页附件。
- “清除全部本地数据”会请求删除附件数据库；浏览器也可能按站点存储策略自行回收 IndexedDB。

## Cloudflare Worker 边界

`_worker.js` 的代理设计目标是“受控跨域转发”，不是通用代理：

- `ALLOWED_UPSTREAMS` 必须显式列出上游 Origin；
- 只允许 HTTPS 上游；
- 只允许 GET 和 POST；
- 请求体上限为 20 MiB，Worker 会读取并复核实际字节数，不能只依赖 `Content-Length`；
- 不允许 URL 内嵌用户名或密码；
- 不自动跟随上游重定向；
- 过滤 hop-by-hop、Cloudflare 和部分浏览器安全头。

不要删除白名单校验或把未知用户可控的 Origin 加入生产环境。对外公开的部署应使用独立、最小权限 API Key，并设置服务商侧费用上限。

## 本机桥接边界

本机桥接：

- 只监听 `127.0.0.1`；
- 不为绕过 Safari 的 HTTPS→HTTP 回环限制而安装自签名根证书、关闭浏览器安全策略、使用公共隧道或监听局域网地址；Safari 中应禁用无效检测并引导到 Edge/Chrome 普通标签页；
- 校验下载时生成的随机 Bearer 令牌；
- 校验发起请求的网页 Origin；
- 自动端口发现先验证与候选端口绑定的 HMAC 挑战证明，再向该端口发送 Bearer 令牌；
- 配对令牌只保存在当前标签页 `sessionStorage`；
- 不读取、复制或上传 CLI 认证文件；
- 只停止命令行可确认属于 AI Shakedown 的进程；
- 在独立临时工作目录调用 CLI。

工具限制：

- Codex：默认 `ephemeral` 临时任务、`read-only` 沙盒与 `never` 审批；只有用户主动打开“同时保存到 Codex”才创建可见任务；
- Gemini CLI：`plan` 审批；
- Claude Code：禁用内置/MCP 工具并使用安全、计划模式；
- OpenCode：注入 `permission: deny` 并禁用分享；
- Antigravity：当前没有等价的已文档化禁用工具参数，应视为 Beta。

任何模型都可能受提示注入影响。本机模式不应被当作通用安全隔离层，尤其不要把不可信网页内容、未知仓库文本或机密环境交给 Antigravity 路径处理。

## 对话与导入

- 对话、System Prompt 和自定义智能体可能包含业务机密，并保存在浏览器本地。
- 导入器会跳过明显的凭据文件名，但不能识别所有敏感内容。
- 导入前由用户选择文件；项目不会主动扫描整个磁盘。
- “完整备份”排除 API Key、连接配置、CLI 登录和配对令牌，但会包含全部对话、System、草稿以及附件内容。
- 完整备份 JSON 没有加密；图片以 base64 保存，不能把它当作脱敏或加密文件。只应通过可信渠道传输并存放在受控位置。
- 完整备份导入只追加并重新生成本地 ID；同一 `backupId` 的同一对话只导入一次，不会覆盖目标浏览器数据。
- 发送到远程模型前，应遵守对应服务商的数据处理与保留政策。

## 输出渲染

AI 回复通过 Marked 解析，再由 DOMPurify 清洗。流式期间以纯文本显示，结束后才渲染 Markdown。

更新 Marked、DOMPurify 或渲染配置时，应重点回归链接、图片、原始 HTML、事件属性、表单、iframe、object/embed 和 URL scheme。

## 智能体预设边界

- 内置智能体只是可选择的 System 提示词，不会为角色自动授予工具、设备、账号或外部服务访问权。
- 健康类项目预设只做信息整理、就医准备和紧急求助导航，不诊断、不替代医生，也不建议用户自行调整处方。
- 法律、税务、财务和合规预设只提供一般信息、材料清单与复核问题；重大决定必须由适用地区的合资格专业人士确认。
- 系统与设备预设优先只读诊断、备份、精确目标、确认和回滚，不应把关闭安全机制作为默认修复。
- 用户可以随时点击当前角色旁的“取消”或角色库中的“取消当前智能体”；该操作清除当前对话的角色 System 定义，但保留已有聊天记录。

## 部署建议

- 使用 HTTPS。
- 把 `ALLOWED_UPSTREAMS` 保持在最小范围。
- 使用独立的低权限 API Key 和服务商预算限制。
- 不要在公开演示站预置真实 Key。
- 定期更新浏览器、本机 CLI 和 Node.js。
- 升级时下载当前版本启动器，避免旧桥接长期驻留。
- 不要把带配对参数的页面地址发给其他人。

## 报告漏洞

请优先使用 GitHub 仓库的 Private vulnerability reporting（如果仓库已启用），并提供：

- 受影响版本与浏览器/系统；
- 可重复的最小步骤；
- 实际影响；
- 建议修复；
- 不含真实凭据的日志或请求样例。

如果私密报告入口不可用，请创建一个不包含利用细节和凭据的普通 Issue，请维护者提供私下沟通方式。不要在公开 Issue、PR、截图或日志中提交 API Key、Bearer Token、本机配对令牌、完整私人对话或个人路径信息。

## 不属于漏洞的情况

- 用户主动把 API Key 保存在当前站点后，同源脚本能读取 localStorage；
- 离线状态不能调用远程模型；
- 普通静态服务器不执行 Cloudflare Worker；
- 本机桥接停止后网页无法继续调用本机 CLI；
- 没有配置 `ALLOWED_UPSTREAMS` 时代理拒绝请求；
- 不支持的第三方 CLI 版本改变参数或输出格式。

[返回文档中心](./docs/README.md) · [部署指南](./docs/DEPLOYMENT.md) · [返回项目首页](./README.md)
